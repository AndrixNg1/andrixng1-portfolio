---
interface Props {
  className?: string;
}

const { className = '' } = Astro.props;
---

<div class={`bg-bg-primary border border-border rounded-3xl p-6 md:p-8 shadow-[var(--card-shadow)] ${className}`}>
  <slot />
</div>
